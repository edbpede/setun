// Setun styles with UnoCSS, not Tailwind. This file exists only because the
// shadcn-svelte CLI's `add` command refuses to run without it; `init` is never run.
// See .agents/rules/svelte5-sveltekit-app.md.
export default {};

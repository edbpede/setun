node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
playwright-report/
blob-report/
.playwright/

# Vitest browser mode writes these on failure
**/__screenshots__/
.vitest-attachments/

# Paraglide (generated from messages/ at install, dev and build)
src/lib/paraglide
project.inlang/cache/

# Local deployment state — never commit an operator's filled-in gateway config
/cpa/config.yaml
!/cpa/config.example.yaml
/mcp.json

# The development database and file storage. Attachments and generated images
# land here, and they are a pupil's own work (PRD §16, §21).
/data

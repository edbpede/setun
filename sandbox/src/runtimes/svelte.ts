/**
 * Pinned, self-hosted Svelte (PRD §13).
 *
 * The same version the application itself is built against, so a component a
 * pupil writes here behaves as one written in the repository would.
 */
export * from "svelte";
